<!DOCTYPE html>
<html lang="en" class="light">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" />
</head>
<body class="bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-gray-100">
    <div class="w-full mx-auto p-8">
        <div class="max-w-4xl mx-auto bg-white dark:bg-gray-800 p-6 rounded-xl shadow-lg">
            <!-- Article title -->
            <h1 class="text-3xl font-bold text-gray-800 dark:text-gray-100 mb-4"><%= article.title %></h1>

            <!-- Author and date -->
            <div class="flex items-center mb-6">
                <img src="<%= article.User.image %>" alt="Author Image" class="w-12 h-12 rounded-full mr-3">
                <div class="flex-1">
                    <h2 class="text-gray-700 dark:text-gray-300 font-semibold"><%= article.User.fullname %></h2>
                    <p class="text-gray-500 text-sm"><%= new Date(article.createdAt).toLocaleDateString() %></p>
                </div>
                <!-- Dropdown Button -->
                <button id="dropdownMenuIconButton" data-dropdown-toggle="dropdownActions" class="inline-flex  items-center p-2 text-sm font-medium text-center text-gray-900 bg-white rounded-lg hover:bg-gray-100 focus:ring-4 focus:outline-none dark:text-white dark:bg-gray-800 dark:hover:bg-gray-700 dark:focus:ring-gray-600" type="button">
                    <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 4 15">
                        <path d="M3.5 1.5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0Zm0 6.041a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0Zm0 5.959a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0Z"/>
                    </svg>
                </button>
                <div id="dropdownActions" class="z-50 hidden bg-white divide-y divide-gray-100 rounded-lg shadow w-40 dark:bg-gray-700 dark:divide-gray-600">
                    <ul class="py-2 text-sm text-gray-700 dark:text-gray-200">
                        
                            <li>
                                <a href="/articles/<%= article.id %>/edit" class="flex items-center justify-between px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">
                                    <span>Edit</span>
                                    <i class="fa-solid fa-pen-to-square"></i>
                                </a>
                            </li>
                            <li>
                                <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="inline">
                                    <button type="submit" class="flex items-center justify-between px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">
                                        <span>Delete</span>
                                        <i class="fa-solid fa-trash"></i>
                                    </button>
                                </form>
                            </li>

                        <li>
                            <a href="#" class="flex items-center justify-between px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">
                                <span>Copy URL</span>
                                <i class="fa-solid fa-clipboard"></i>
                            </a>
                        </li>
                        <li>
                            <a href="#" class="flex items-center justify-between px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">
                                <span>Save</span>
                                <i class="fa-regular fa-bookmark"></i>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>

            <!-- Cover Image -->
            <% if (article.coverImage) { %>
            <div class="cover-image mb-10">
                <img src="<%= article.coverImage %>" alt="<%= article.title %>" class="w-full rounded-xl" />
            </div>
            <% } %>

            <!-- Article content -->
            <div class="article-content text-gray-700 dark:text-gray-300 leading-relaxed mb-8">
                <%- article.content %>
            </div>

            <!-- Author bio -->
            <div class="author-bio p-6 bg-gray-200 dark:bg-gray-700 rounded-lg shadow-lg">
                <h3 class="font-semibold text-xl text-gray-800 dark:text-gray-100 mb-4">About the Author</h3>

                <div class="flex items-center mb-4">
                    <img src="<%= article.User.image %>" alt="Author Image" class="w-14 h-14 rounded-full border-2 border-indigo-500 mr-3">
                    <h2 class="text-gray-800 dark:text-gray-300 font-semibold text-lg"><%= article.User.fullname %></h2>
                </div>

                <p class="text-gray-600 dark:text-gray-300 mb-2 leading-relaxed"><%= article.User.bio %></p>

                <p class="text-gray-500 dark:text-gray-400">
                    Registered on: <% const registrationDate = new Date(article.User.createdAt); const formattedDate = registrationDate instanceof Date && !isNaN(registrationDate) ? registrationDate.toLocaleDateString() : 'Unknown date'; %> <%= formattedDate %>
                </p>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/flowbite@2.5.1/dist/flowbite.min.js"></script>

</body>
</html>
